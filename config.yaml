llm:
  models:
    - name: gpt-5.5
      provider: openai-compatible
      api_key: ${APP_LLM_API_KEY}
      base_url: ${APP_LLM_BASE_URL}
      model: gpt-5.5
      priority: 1
    - name: qwen-tools
      provider: openai-compatible
      api_key: ${APP_QWEN_API_KEY}
      base_url: ${APP_QWEN_BASE_URL}
      model: qwen2.5:7b
      priority: 10

agent:
  tool_model: qwen-tools
